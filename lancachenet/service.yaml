apiVersion: v1
kind: Service
metadata:
  name: lancache-monolithic
spec:
  type: LoadBalancer
  selector:
    app: lancache-monolithic
  ports:
    - port: 80
      targetPort: http
    - port: 443
      targetPort: https
